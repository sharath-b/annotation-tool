#!/usr/bin/env node

const swaggerMerger = require('./swagger-merger')

swaggerMerger.merge({
  input: 'test-spec.json',
  output: 'swagger.json',
  compact: false
}).catch(e => {
  console.error(e)
})
